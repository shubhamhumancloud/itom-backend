# HTTP server
PORT=3000

# Postgres (point at your locally-installed Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=itom
DB_PASSWORD=itom
DB_NAME=itom

# Set to 'true' to log every SQL query (noisy, dev only)
DB_LOGGING=false
